// https://school.programmers.co.kr/learn/courses/30/lessons/118666

function solution(order) {
    
    const container = Array.from({length: order.length}, (_,i) => i+1);
    let subContainer = [], result = 0;

    for(let i = 0; i < order.length; i++) {
        container.
        while(order.length && order[i])
        if(subContainer.pop() === order[i]) result++;
        else return result;
    }

    return result;
}